{
  "name": "qwts-agent-inventory",
  "private": true,
  "description": "Report-only dependency and tooling inventory for the qwts fleet: a reusable GitHub Actions workflow, the zero-dependency normalizer behind it, and the weekly fleet catalog",
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  },
  "scripts": {
    "inventory": "node tools/dependency-inventory/inventory.mjs",
    "test": "node --test tools/dependency-inventory/tests/inventory.test.mjs",
    "lint:markdown": "markdownlint docs/ AGENTS.md README.md"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.49.1"
  }
}
